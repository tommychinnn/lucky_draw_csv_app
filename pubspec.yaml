name: lucky_draw_csv_app
description: "A Lucky Draw application"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter
  file_picker: ^6.1.1
  csv: ^5.1.1
  path_provider: ^2.1.2
  cupertino_icons: ^1.0.2
  just_audio: ^0.9.36
  provider: ^6.1.1
  js: ^0.6.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/audio/combine_slotmachine_winner.mp4
    - assets/images/logo.png